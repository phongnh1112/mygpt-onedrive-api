services:
  - type: web
    name: mygpt-onedrive-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn excel_user_analysis:app
    plan: free
    branch: main
